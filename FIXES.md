# Исправления для админки и загрузки данных

## ✅ Что было исправлено

1. **Создан отдельный endpoint для админки** (`/api/admin/creatives`)
   - Возвращает все креативы (не только published)
   - Поддерживает фильтрацию по статусу
   - Правильный формат данных для админки

2. **Обновлена функция `applyFilters` в админке**
   - Использует новый API endpoint вместо Supabase
   - Правильная фильтрация по всем параметрам

3. **Обновлена функция `loadData` в админке**
   - Использует `/api/admin/creatives` для загрузки всех креативов
   - Правильная загрузка справочников через `/api/references/[type]`

4. **Исправлено создание креатива**
   - Добавлена проверка на существование справочников
   - Исправлена обработка ошибок

## 🔧 Команды для обновления на сервере

```bash
cd /var/www/spy-dashboard
git pull
npm run build
pm2 restart spy-dashboard
```

## 📋 Проверка после обновления

1. **Проверка загрузки стран:**
   ```bash
   curl http://localhost:3000/api/references/countries
   ```
   Должен вернуть все страны из базы данных.

2. **Проверка админки:**
   - Откройте `/admin`
   - Проверьте, что загружаются все креативы (draft и published)
   - Проверьте фильтры

3. **Проверка создания креатива:**
   - Создайте креатив через админку
   - Проверьте, что он появился в списке

## 🐛 Если страны не загружаются

Проверьте базу данных:
```bash
psql -h localhost -U spyservice -d spyservice_db -c "SELECT COUNT(*) FROM countries;"
```

Должно быть 61 страна. Если меньше, выполните:
```bash
psql -h localhost -U spyservice -d spyservice_db -f database_schema.sql
```

