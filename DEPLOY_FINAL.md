# Финальный деплой проекта на Beget

## ✅ Что было сделано

1. ✅ Заменен Supabase на PostgreSQL
2. ✅ Заменен Supabase Storage на S3 (Beget S3)
3. ✅ Обновлены все API routes
4. ✅ Обновлен клиентский код для работы с новыми API
5. ✅ Проект собран и запущен на сервере

## 🚀 Команды для финального деплоя

### 1. Обновление кода на сервере

```bash
cd /var/www/spy-dashboard
git pull
```

Или если файлы изменены локально, загрузите их через SFTP.

### 2. Пересборка проекта

```bash
npm install
npm run build
```

### 3. Перезапуск приложения

```bash
pm2 restart spy-dashboard
```

### 4. Проверка работы

```bash
pm2 status
pm2 logs spy-dashboard
```

## 📋 Проверка функционала

### Основной дашборд
- [ ] Открывается главная страница
- [ ] Загружаются фильтры (форматы, типы, страны, платформы)
- [ ] Работает фильтрация креативов
- [ ] Работает пагинация

### Админ панель
- [ ] Открывается `/admin` с паролем `spyservice2024`
- [ ] Загружаются справочники
- [ ] Работает создание креатива
- [ ] Работает модерация (approve/draft)
- [ ] Работает удаление креативов
- [ ] Работает редактирование креативов

### API endpoints
- [ ] `/api/creatives` - GET и POST
- [ ] `/api/references/formats` - GET
- [ ] `/api/references/types` - GET
- [ ] `/api/references/placements` - GET
- [ ] `/api/references/platforms` - GET
- [ ] `/api/references/countries` - GET
- [ ] `/api/admin/moderate` - POST и GET
- [ ] `/api/admin/delete-creatives` - POST
- [ ] `/api/admin/update-creative` - POST

## 🔧 Если что-то не работает

### Ошибка загрузки справочников

Проверьте API:
```bash
curl http://localhost:3000/api/references/formats
```

Должен вернуть JSON с данными.

### Ошибка создания креатива

Проверьте логи:
```bash
pm2 logs spy-dashboard --err
```

Проверьте подключение к БД:
```bash
psql -h localhost -U spyservice -d spyservice_db -c "SELECT COUNT(*) FROM creatives;"
```

### Ошибка загрузки файлов в S3

Проверьте переменные окружения:
```bash
cat .env.production | grep S3
```

Проверьте подключение к S3 (в коде приложения).

## 📝 Важные файлы

- `.env.production` - переменные окружения
- `ecosystem.config.js` - конфигурация PM2
- `database_schema.sql` - схема БД
- `/var/log/pm2/` - логи приложения

## ✅ Готово!

Проект должен полностью работать на `https://profitlabspy.com`

