# Spy Service Dashboard

Веб-дашборд для мониторинга рекламных креативов, аналог f5Spy.

## 🚀 Функции

### Основной дашборд
- **Фильтры** как на f5Spy (дата, формат, тип, страна, платформа, cloaking)
- **Сетка креативов** с превью изображений/видео
- **Модальные окна** с детальной информацией
- **Пагинация** результатов
- **Адаптивный дизайн** (мобильные, планшеты, десктоп)

### Рекламные блоки
- **Встроенная реклама** между карточками креативов
- **Ротация рекламы** на основе позиции
- **Скрытое размещение** (выглядит как обычный контент)

### Админ-панель (скрытая)
- **Доступ**: `/admin` с паролем `spyservice2024`
- **Управление креативами**: просмотр, удаление
- **Статистика**: общее количество, сегодняшние, страны, платформы
- **Управление рекламой**: (в разработке)

## 🛠️ Технологии

- **Next.js 15** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **Supabase** (база данных + storage)
- **Lucide React** (иконки)

## 📁 Структура проекта

```
src/
├── app/
│   ├── admin/          # Скрытая админ-панель
│   ├── globals.css     # Глобальные стили
│   ├── layout.tsx      # Основной лайаут
│   └── page.tsx        # Главная страница
├── components/
│   ├── AdBlock.tsx     # Рекламный блок
│   ├── CreativeCard.tsx # Карточка креатива
│   ├── CreativeModal.tsx # Модальное окно
│   └── Filters.tsx     # Компонент фильтров
└── lib/
    ├── supabase.ts     # Клиент Supabase + типы
    └── utils.ts        # Утилиты
```

## 🔧 Настройка

### 1. Установка зависимостей
```bash
npm install
```

### 2. Переменные окружения
Файл `.env.local` уже настроен с ключами Supabase.

### 3. Запуск
```bash
npm run dev
```

Откройте http://localhost:3000

## 📊 База данных

Подключается к Supabase с таблицами:
- `creatives` - основная таблица креативов
- `formats` - форматы (teaser, video, banner, image)
- `types` - типы/вертикали (crypt, gambling, nutra, etc.)
- `placements` - размещения (demand_gen, uac, youtube_ads, etc.)
- `platforms` - платформы (web, google, youtube, facebook, etc.)
- `countries` - страны (код ISO + название)

## 🎨 Дизайн

Темная тема в стиле f5Spy:
- **Цвета**: серые оттенки (#0a0a0a, #1a1a1a, #404040)
- **Акценты**: синий (#3b82f6)
- **Сетка**: 1-5 колонок в зависимости от экрана
- **Анимации**: hover эффекты, плавные переходы

## 🔐 Админ-панель

**URL**: `/admin`
**Пароль**: `spyservice2024`

Функции:
- Просмотр всех креативов в табличном виде
- Удаление креативов
- Статистика по количеству
- Управление рекламными блоками (в разработке)

## 🚀 Деплой

### GitHub + Vercel
1. Создать репозиторий на GitHub
2. Загрузить код: `git push origin main`
3. Подключить к Vercel
4. Добавить переменные окружения в Vercel
5. Автодеплой готов!

### Переменные для Vercel
```
NEXT_PUBLIC_SUPABASE_URL=https://oilwcbfyhutzyjzlqbuk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

## 📱 API для мобильных приложений

Используйте Supabase REST API:
```
POST /rest/v1/creatives
GET  /rest/v1/formats
GET  /rest/v1/types
GET  /rest/v1/countries
```

Пример загрузки креатива:
```javascript
const { data, error } = await supabase
  .from('creatives')
  .insert({
    title: 'Casino Ad',
    format_id: 'uuid-format',
    type_id: 'uuid-type',
    country_code: 'DE',
    platform_id: 'uuid-platform',
    media_url: 'path/to/image.jpg'
  })
```

## 🔄 Следующие шаги

- [ ] Создать мобильное приложение для загрузки креативов
- [ ] Расширить админ-панель (управление рекламой)
- [ ] Добавить аналитику и метрики
- [ ] Настроить CI/CD pipeline
- [ ] Добавить тесты
